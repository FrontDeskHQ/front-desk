{
  "setup-worktree-unix": [
    "bun install",
    "bash -c 'for f in .env apps/api/.env.local apps/web/.env.local apps/worker/.env.local connectors/discord/.env.local connectors/slack/.env.local connectors/github/.env.local; do if [ -f \"$ROOT_WORKTREE_PATH/$f\" ]; then cp \"$ROOT_WORKTREE_PATH/$f\" \"$f\"; fi; done'"
  ],
  "setup-worktree-windows": [
    "bun install"
  ],
  "setup-worktree": [
    "bun install"
  ]
}
